1. Idea
	1. flowchart
	2. class diagram
2. init template
3. start

`uv init flashcards_ai_obsidian_pro`
`cd <name project>`
`pycharm .`
github -> new respository
`git remote add origin <SSH>`

```zsh
 3177  pip index versions pytest
 3178  uv add "pytest==9.0.2" --dev
 3179  pip index versions ruff
 3180  uv add "ruff==0.14.10" --dev
 3182  uv add "pytest-cov" --dev
 3183  pip index versions mypy
 3184  uv add "mypy==1.19.1" --dev
```

konfiguracja templatki 


