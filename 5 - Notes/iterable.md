	__iter__
obiekt jest iterowalny (iterable) jeśli implementuje metodę

`def __iter__(self): ...`

Ta metoda **musi zwracać iterator object** (czyli obiekt, który implementuje __next__() i __iter__())
